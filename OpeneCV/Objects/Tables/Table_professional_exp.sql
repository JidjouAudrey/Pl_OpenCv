CREATE TABLE Professional_exp (
       id_expro INT, 
       status_expro VARCHAR(20),
       startDate DATE, 
       endDate DATE,
       organisation VARCHAR(20),  
       function_exp VARCHAR(50), 
       id_exp INT);